FROM node:20-alpine
WORKDIR /app
# Copy only the server folder (backend code, its own package.json, index.js, etc.)
COPY server/ ./
RUN npm ci --only=production
EXPOSE 5000
ENV NODE_ENV=production
ENV PORT=5000
CMD ["node", "index.js"]