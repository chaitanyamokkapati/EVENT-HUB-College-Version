# EventHub Server Environment Variables
# Copy this file to .env and fill in the values
# ⚠️  NEVER commit real credentials to version control

# ===========================================
# Server Configuration
# ===========================================
NODE_ENV=development
PORT=5001

# ===========================================
# Database Configuration (REQUIRED)
# ===========================================
# Choose ONE of the following based on your setup:
#
# 1. MongoDB Atlas (Cloud - Recommended for production)
#    mongodb+srv://username:password@cluster.mongodb.net/eventhub?retryWrites=true&w=majority
#
# 2. Local MongoDB (Development)
#    mongodb://localhost:27017/eventhub
#
# 3. Linux Server with Public IP (cPanel compatible)
#    mongodb://admin:password@192.168.x.x:27017/eventhub?authSource=admin
#    Notes:
#    - Replace 192.168.x.x with your server's actual IP
#    - authSource=admin required if using admin authentication
#    - Ensure MongoDB is accessible from frontend server IP
#    - Configure firewall rules to allow port 27017 access
#
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/eventhub?retryWrites=true&w=majority

# ===========================================
# Security
# ===========================================

# JWT Secret for token signing (CHANGE IN PRODUCTION!)
# Generate: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_jwt_secret_key_change_in_production

# QR Code Secret for verification (CHANGE IN PRODUCTION!)
QR_CODE_SECRET=your_qr_code_secret_key_change_in_production

# ===========================================
# CORS Configuration (for public IP backends)
# ===========================================
# Option 1: Allow specific origins (RECOMMENDED)
# Comma-separated list of allowed frontend URLs
# CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com,http://192.168.0.100:5173
#
# Option 2: Allow all origins from your website URL
# WEBSITE_URL=https://yourdomain.com
#
# Leave both empty for development (allows all origins)
#
CORS_ORIGINS=
WEBSITE_URL=http://localhost:5173

# ===========================================
# Email Configuration (Gmail SMTP)
# ===========================================
# 1. Enable 2-Step Verification on your Gmail account
# 2. Generate App Password: https://myaccount.google.com/apppasswords
# 3. Use the 16-character password below (remove spaces)
#
GMAIL_USER=your.email@gmail.com
GMAIL_APP_PASSWORD=your_16_character_app_password

# Application name (used in email templates and notifications)
APP_NAME=EventHub

# ===========================================
# Production Deployment Settings
# ===========================================
# Set these when deploying to production

# Frontend URL (used for email links and redirects)
FRONTEND_URL=http://localhost:5173

# Full website URL (used in email templates)
WEBSITE_URL=http://localhost:5173

# CORS allowed origins (comma-separated, for production)
# Leave empty for development (allows all origins)
# Production example: https://eventhub.com,https://www.eventhub.com
CORS_ORIGINS=

# Force HTTPS redirect (set to true in production)
FORCE_HTTPS=false

# ===========================================
# Health Monitoring & Alerts
# ===========================================
# Email addresses to receive health monitoring alerts
# Comma-separated list of emails
# These emails receive critical system alerts (CPU, memory, DB issues)
# Does NOT send to all admins - only to explicitly configured emails
#
# Example: HEALTH_MONITOR_EMAILS=devops@company.com,admin@company.com,sysadmin@company.com
HEALTH_MONITOR_EMAILS=

