# ============================================================================
# EventHub Frontend Environment Variables
# ============================================================================
# 
# Copy this file to .env and configure VITE_API_BASE_URL
# For production deployment, also use .env.production
#
# CORE PRINCIPLE:
# Backend IP/port/hostname changes require ONLY editing .env file.
# Zero frontend code changes necessary.
# ============================================================================

# ============================================================================
# Backend Configuration (SINGLE SOURCE OF TRUTH)
# ============================================================================

# Backend Base URL - Your Node.js backend server address
# This is the ONLY environment variable that controls backend connectivity.
# Format: http://IP_OR_HOSTNAME:PORT (no trailing slash)
# 
# Configuration Examples by Scenario:
# 
# 1. LOCAL DEVELOPMENT (backend on same machine):
#    VITE_API_BASE_URL=http://localhost:5001
#
# 2. LAN/NETWORK DEVELOPMENT (backend on different machine):
#    VITE_API_BASE_URL=http://192.168.1.100:5001
#    (Find backend machine IP: use `ipconfig` on Windows, `ifconfig` on Linux/Mac)
#
# 3. VIRTUALBOX/VM DEVELOPMENT:
#    VITE_API_BASE_URL=http://192.168.x.x:5001
#    (Replace with your VM's network IP, usually 192.168.x.x)
#
# 4. PRODUCTION WITH HTTPS & DOMAIN:
#    VITE_API_BASE_URL=https://api.example.com
#
# 5. PRODUCTION WITH HTTPS & PUBLIC IP:
#    VITE_API_BASE_URL=https://203.0.113.42:5001
#
# 6. PRODUCTION WITH HTTP (NOT recommended):
#    VITE_API_BASE_URL=http://203.0.113.42:5001
#    (Use HTTPS in production unless testing only)
#
# HOW TO CHANGE BACKEND:
# 1. Edit this value
# 2. Stop frontend dev server (Ctrl+C)
# 3. Restart: npm run dev
# 4. Frontend automatically connects to new backend
# 5. NO code changes needed
#
VITE_API_BASE_URL=http://localhost:5001
